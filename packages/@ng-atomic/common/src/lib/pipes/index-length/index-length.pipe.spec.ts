import { IndexLengthPipe } from './index-length.pipe';

describe('IndexLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
