import { ResolveColumnsPipe } from './resolve-columns.pipe';

describe('ResolveColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
