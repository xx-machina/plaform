import { AutoColumnsPipe } from './auto-columns.pipe';

describe('AutoColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
